populate_blob() {
  echo " => Populating project/device-specific blobs"
  echo "  -> This function should be overloaded in the corresponding project/device"
  echo " => Populated end as there's nothing to do"
}
